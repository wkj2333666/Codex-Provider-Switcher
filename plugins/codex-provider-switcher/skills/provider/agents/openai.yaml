interface:
  display_name: "Provider"
  short_description: "Inspect or switch this Codex task's provider"
  default_prompt: "Use /provider status or /provider switch <name>."
policy:
  allow_implicit_invocation: false
