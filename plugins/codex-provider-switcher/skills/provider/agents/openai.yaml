interface:
  display_name: "Provider"
  short_description: "Switch the provider for this Codex task"
  default_prompt: "Use $provider to switch this task to a configured provider."
policy:
  allow_implicit_invocation: false
